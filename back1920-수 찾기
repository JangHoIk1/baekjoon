import sys
N = int(input())
N_A = list(map(int,sys.stdin.readline().split()))
M = int(input())
M_A = list(map(int,sys.stdin.readline().split()))
i=0

while M>0:
    if M_A[i] in N_A:
        print("1") 
    else:
        print("0")
    i+=1
    M-=1
    
